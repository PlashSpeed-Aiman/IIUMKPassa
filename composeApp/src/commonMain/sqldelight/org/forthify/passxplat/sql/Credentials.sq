CREATE TABLE Credentials (
    username TEXT  PRIMARY KEY,
    password TEXT
);

insertCredentials:
INSERT OR REPLACE INTO Credentials(username, password) VALUES (?, ?);

getCredentials:
SELECT * FROM Credentials LIMIT 1;